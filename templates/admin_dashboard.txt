<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Dashboard - Outlet Portal</title>
    <link rel="stylesheet" href="{{ url_for('static', filename='style2.css') }}">
</head>
<body>
    <h1>Welcome, {{ user.EMP_NAME }}</h1>

    <!-- Sidebar -->
    <div class="sidebar">
        <h3>MAIN DASHBOARD</h3>
        <a class="active" href="#">Dashboard</a>

       <h3>APPLICATIONS MANAGEMENT</h3>
        <a href="#">Add New Outlet</a>
        <a href="#">All Outlets</a>
        <a href="{{ url_for('outlet_survey') }}">Outlet Survey</a> <!-- New Link -->

        <h3>USER MANAGEMENT</h3>
        <a href="#">Add Employee</a>
        <a href="#">Manage Employee</a>
    </div>

    <!-- Topbar -->
    <div class="topbar">
        <div><strong>Dashboard Outlet Portal</strong></div>
        <div class="user">
            <img src="https://cdn-icons-png.flaticon.com/512/3135/3135715.png" alt="user-icon">
            <div>
                <div>{{ user.EMP_NAME }}</div>
                <small>Outlet Portal</small>
            </div>
        </div>
    </div>

    <!-- Main Content -->
    <div class="main">
        <div class="header">Dashboard Outlet Portal</div>
        <div class="sub-header">Manage All Data</div>

        <div class="card-container">
            <div class="card" style="border-left-color: #3f51b5;">
                <h4>Outlets</h4>
                <p>Manage All Outlets</p>
                <div class="count">{{ outlet_survey_count }}</div>
            </div>

            <div class="card" style="border-left-color: #2196f3;">
                <h4>Completed</h4>
                <p>Manage All Completed</p>
                <div class="count">{{ complete_survey_count }}</div>
            </div>

            <div class="card" style="border-left-color: #4caf50;">
                <h4>Employee</h4>
                <p>Manage Employee</p>
                <div class="count">{{ employee_count }}</div>
            </div>
        </div>
    </div>

</body>
</html>
